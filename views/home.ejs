<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Map</title>
    <link rel="icon" href="/assets/gail.png" type="image/x-icon" >


    <link
    href="https://cdn.jsdelivr.net/npm/remixicon@4.3.0/fonts/remixicon.css"
    rel="stylesheet"
/>
    <link rel="stylesheet" href="https://unpkg.com/leaflet/dist/leaflet.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
   <% if (!admin) { %>
    <link rel="stylesheet" href="/stylesheets/map-user.css">
   <% }else{ %>
    
   <link rel="stylesheet" href="/stylesheets/map-admin.css">
<% } %>
  
</head>
<body>
    
    <% if(admin){ %>
    <%- include('partials/sidebar') %>
<% } %>
<div class="con">
    <% if(admin){ %>
        <h1>Map</h1>
    <% } %>
       
        <% if(!admin){ %>
            <div class="head">
            <h1>Map</h1>
            <form action="/logout" method="post">

                <button type="submit" class="btn btn-danger log ">Logout</button>
            </form>
        </div>
        <% } %>
    
    
    <div id="map"></div>
    
    <% if (!admin) { %>
        
        <div class="info-box1">
            <p id="para">Your logs...</p>
        </div>
       
        <div class="info-box">
            <p>Your Id: </p>
            <p id="choco"><%= userId %></p>
        </div>
        
        <div class="but">
        
     <button id="btnn" class="btn btn-primary btn-location">Find My Location</button>
     <a href="/user/new/temp" id="btnn" class="btn btn-primary btn-location">Manual Checkin</a>
     
    </div>
      
    <% }else{ %>
    <div class="info-box">
        <p class="texxt">Current Users Online: <br> <br><i class="ri-user-smile-line"></i><span class="sp"> <%= onlineUsers %> </span></p>
    </div>
    <div class="info-box1">
        <p class="texxt">Current Users Offline: <br><br><i class="ri-emotion-sad-line"></i><span class="sp"> <%= offlineUsers %> </span></p>
    </div>
    <div class="info-box2">
        <p class="texxt">Total Fence: <br><br><i class="ri-building-4-line"></i><span class="sp"> <%= totalFence %> </span></p>
    </div>
    <% } %>
</div>
    <script src="https://unpkg.com/leaflet/dist/leaflet.js"></script>
    <script src="https://unpkg.com/leaflet@1.7.1/dist/leaflet.js"></script>
    <script src="/socket.io/socket.io.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <% if(admin){ %>
        <script src="/js/app2.js"></script>
    <% }else{ %>
    <script src="/js/app.js"></script>
    <% } %>
</body>
</html>
